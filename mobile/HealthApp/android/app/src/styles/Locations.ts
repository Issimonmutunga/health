import { StyleSheet } from 'react-native';

const Locations = StyleSheet.create({
    container: {
      flex: 1,
    },
    map: {
      flex: 1,
    },
    detailsContainer: {
      padding: 10,
      backgroundColor: '#fff',
      borderTopWidth: 1,
      borderColor: '#ccc',
    },
    locationDetails: {
      fontSize: 16,
      fontWeight: 'bold',
    },
  });

export default Locations;
